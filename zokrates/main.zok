from "./binarize" import binarize;
from "./make_words.zok" import compute_word;

//TODO: fill in with the real main function when we have more pieces
def main(private field a, private field b) {
    assert(a == b * b);
}
